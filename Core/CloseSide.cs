namespace RiseOn.NativeAdMob {
    /// <summary>
    /// Which edge a control sits on. The ordinals cross into the native
    /// layers, so they are spelled out rather than left to declaration order.
    /// </summary>
    public enum CloseSide {
        Left   = 0
      , Right  = 1
      , Random = 2
    }
}
